name: Conservación de Islas
on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main
jobs:
  actions:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Build image
      run: docker build --tag islasgeci .
    - name: Check formatting
      run: docker run islasgeci make check
    - name: Run tests
      env:
        BITBUCKET_EMAIL: ${{ secrets.BITBUCKET_EMAIL }}
        BITBUCKET_API_TOKEN: ${{ secrets.BITBUCKET_API_TOKEN }}
      run: docker run islasgeci make init --env BITBUCKET_EMAIL=${BITBUCKET_EMAIL} --env BITBUCKET_API_TOKEN=${BITBUCKET_API_TOKEN}
    - name: Evaluate mutation resistance
      env:
        BITBUCKET_EMAIL: ${{ secrets.BITBUCKET_EMAIL }}
        BITBUCKET_API_TOKEN: ${{ secrets.BITBUCKET_API_TOKEN }}
      run: docker run islasgeci make mutants --env BITBUCKET_EMAIL=${BITBUCKET_EMAIL} --env BITBUCKET_API_TOKEN=${BITBUCKET_API_TOKEN}
    - name: Create/Update tag
      uses: rickstaa/action-create-tag@v1.5.4
      with:
         tag: "latest"
         message: "Latest release"
         force_push_tag: true
    - name: Set up Python 3.9
      uses: actions/setup-python@master
      with:
        python-version: 3.9
    - name: Install pypa/build
      run: >-
        python -m
        pip install
        build
        --user
    - name: Build a binary wheel and a source tarball
      run: >-
        python -m
        build
        --sdist
        --wheel
        --outdir dist/
        .
    - name: Publish distribution 📦 to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        password: ${{ secrets.PYPI_TOKEN }}
        skip_existing: true